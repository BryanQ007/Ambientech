export interface MapaState {
  vistaVer: boolean;
  vistaCrear: boolean;
}

export const initialState: MapaState = {
  vistaVer: false,  // Inicializa en false
  vistaCrear: false, // Inicializa en false
};
